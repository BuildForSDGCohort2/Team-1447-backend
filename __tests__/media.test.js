const app = require('../app');
const cloudinary = require('../middlewares/cloudinary-config');

describe( "MediaCtrl", () => {
    describe( "User can create media" , async () => {
        test( "POST/create/media" , async () => {
            
        })
    })
})
